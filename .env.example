# 仅为配置示例；程序只读取环境变量，不自动加载此文件。
POSTGRES_DSN=postgres://chatdesk:replace-me@127.0.0.1:5432/chatdesk?sslmode=disable
HTTP_ADDR=127.0.0.1:8080
LOG_LEVEL=info
DEV_AUTH_ENABLED=false
# 显式开启开发身份时需填写非零 UUID。
DEV_USER_ID=
# 可选：实库测试需有 CREATEDB 权限的账号；缺省跳过，测试本身不启动 Docker。
CHATDESK_TEST_ADMIN_DSN=
